apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.sensorState.name }}
  labels:
    app: {{ .Values.sensorState.name }}
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.sensorState.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.sensorState.name }}
        tier: backend
    spec:
      containers:
      - name: {{ .Values.sensorState.name }}
        image: {{ .Values.sensorState.name }}:1.6
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: {{ .Values.environment }}
        - name: rabbitMq__hostName
          value: {{ .Values.rabbitMq.hostName }}
        - name: rabbitMq__virtualHost
          value: {{ .Values.rabbitMq.virtualHost }}
        - name: rabbitMq__userName
          value: {{ .Values.rabbitMq.userName }}
        - name: rabbitMq__password
          value: {{ .Values.rabbitMq.password }}
        - name: rabbitMq__protocol
          value: {{ .Values.rabbitMq.protocol }}
        - name: rabbitMq__connectionName
          value: {{ .Values.sensorState.rabbitMq.connectionName }}
        - name: sensorRabbitMq__hostName
          value: {{ .Values.sensorRabbitMq.hostName }}
        - name: sensorRabbitMq__virtualHost
          value: {{ .Values.sensorRabbitMq.virtualHost }}
        - name: sensorRabbitMq__userName
          value: {{ .Values.sensorRabbitMq.userName }}
        - name: sensorRabbitMq__password
          value: {{ .Values.sensorRabbitMq.password }}
        - name: sensorRabbitMq__protocol
          value: {{ .Values.sensorRabbitMq.protocol }}
        - name: sensorRabbitMq__connectionName
          value: {{ .Values.sensorState.sensorRabbitMq.connectionName }}
        imagePullPolicy: Never
        ports:
        - containerPort: 80
        startupProbe:
          httpGet:
            path: /system/health/ready
            port: 80
          failureThreshold: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.sensorState.name }}
  labels:
    app: {{ .Values.sensorState.name }}
    tier: backend
spec:
  selector:
    app: {{ .Values.sensorState.name }}
  type: LoadBalancer
  ports:
  - port: {{ .Values.sensorState.port }}
    targetPort: 80
    protocol: TCP
    name: http
