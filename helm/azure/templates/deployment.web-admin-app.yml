apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.webAdminApp.name }}
  labels:
    app: {{ .Values.webAdminApp.name }}
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.webAdminApp.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.webAdminApp.name }}
        tier: backend
    spec:
      containers:
      - name: {{ .Values.webAdminApp.name }}
        image: {{ .Values.registry }}/{{ .Values.webAdminApp.image.name }}:{{ .Values.webAdminApp.image.tag }}
        env:
        - name: DATABASE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: basic-auth
              key: databasePassword
        - name: ASPNETCORE_ENVIRONMENT
          value: {{ .Values.environment }}
        - name: ASPNETCORE_URLS
          value: "http://+"
        - name: serviceUrls__DagAir.AdminNode
          value: "http://admin-node:8088/"
        - name: ConnectionStrings__DefaultConnection
          value: "server={{ .Values.database.server }};database={{ .Values.webAdminApp.database.schema }};user={{ .Values.database.user }};password=$(DATABASE_PASSWORD);SslMode=Required;SslCa=DigiCertGlobalRootCA.crt.pem"
        - name: environment
          value: {{ .Values.webAdminApp.environment }}
        - name: basePath
          value: "/webadminapp"
        imagePullPolicy: Always
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.webAdminApp.name }}
  labels:
    app: {{ .Values.webAdminApp.name }}
    tier: backend
spec:
  selector:
    app: {{ .Values.webAdminApp.name }}
  type: LoadBalancer
  ports:
  - port: {{ .Values.webAdminApp.port }}
    targetPort: 80
    protocol: TCP
    name: http