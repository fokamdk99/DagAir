apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.sensors.name }}
  labels:
    app: {{ .Values.sensors.name }}
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.sensors.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.sensors.name }}
        tier: backend
    spec:
      containers:
      - name: {{ .Values.sensors.name }}
        image: {{ .Values.sensors.name }}:1.6
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: {{ .Values.environment }}
        - name: ConnectionStrings__DagAir.Sensors
          value: "server={{ .Values.database.server }};database={{ .Values.sensors.database.schema }};user={{ .Values.database.user }};password="
        - name: ConnectionKeys__DagAir.Sensors
          value: {{ .Values.database.password }}
        - name: basePath
          value: "/sensors"
        imagePullPolicy: Never
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.sensors.name }}
  labels:
    app: {{ .Values.sensors.name }}
    tier: backend
spec:
  selector:
    app: {{ .Values.sensors.name }}
  type: LoadBalancer
  ports:
  - port: {{ .Values.sensors.port }}
    targetPort: 80
    protocol: TCP
    name: http
