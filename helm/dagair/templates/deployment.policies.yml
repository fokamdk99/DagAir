apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.policies.name }}
  labels:
    app: {{ .Values.policies.name }}
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.policies.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.policies.name }}
        tier: backend
    spec:
      containers:
      - name: {{ .Values.policies.name }}
        image: {{ .Values.policies.name }}:1.6
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: {{ .Values.environment }}
        - name: ConnectionStrings__DagAir.Policies
          value: "server={{ .Values.database.server }};database={{ .Values.policies.database.schema }};user={{ .Values.database.user }};password="
        - name: ConnectionKeys__DagAir.Policies
          value: {{ .Values.database.password }}
        - name: basePath
          value: "/policies"
        imagePullPolicy: Never
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.policies.name }}
  labels:
    app: {{ .Values.policies.name }}
    tier: backend
spec:
  selector:
    app: {{ .Values.policies.name }}
  type: LoadBalancer
  ports:
  - port: {{ .Values.policies.port }}
    targetPort: 80
    protocol: TCP
    name: http
