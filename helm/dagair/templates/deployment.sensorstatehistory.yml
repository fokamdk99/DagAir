apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.sensorStateHistory.name }}
  labels:
    app: {{ .Values.sensorStateHistory.name }}
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.sensorStateHistory.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.sensorStateHistory.name }}
        tier: backend
    spec:
      containers:
      - name: {{ .Values.sensorStateHistory.name }}
        image: {{ .Values.sensorStateHistory.name }}:1.6
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: {{ .Values.environment }}
        - name: rabbitMq__hostName
          value: {{ .Values.rabbitMq.hostName }}
        - name: rabbitMq__virtualHost
          value: {{ .Values.rabbitMq.virtualHost }}
        - name: rabbitMq__userName
          value: {{ .Values.rabbitMq.userName }}
        - name: rabbitMq__password
          value: {{ .Values.rabbitMq.password }}
        - name: rabbitMq__protocol
          value: {{ .Values.rabbitMq.protocol }}
        - name: rabbitMq__connectionName
          value: {{ .Values.sensorStateHistory.rabbitMq.connectionName }}
        - name: DagAirInfluxConfiguration__token
          value: {{ .Values.influxDb.token }}
        - name: DagAirInfluxConfiguration__org
          value: {{ .Values.influxDb.org }}
        - name: DagAirInfluxConfiguration__bucketName
          value: {{ .Values.influxDb.bucketName }}
        - name: DagAirInfluxConfiguration__url
          value: {{ .Values.influxDb.url }}
        imagePullPolicy: Never
        ports:
        - containerPort: 80
        startupProbe:
          httpGet:
            path: /system/health/ready
            port: 80
          failureThreshold: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.sensorStateHistory.name }}
  labels:
    app: {{ .Values.sensorStateHistory.name }}
    tier: backend
spec:
  selector:
    app: {{ .Values.sensorStateHistory.name }}
  type: LoadBalancer
  ports:
  - port: {{ .Values.sensorStateHistory.port }}
    targetPort: 80
    protocol: TCP
    name: http
